name: llm_tag_parser
description: A robust, reactive streaming tag parser for LLM streams that isolates thought, code, or custom blocks chunk-by-chunk in real-time.
version: 0.1.0
repository: https://github.com/ComsIndeed/llm-stream-parsers/tree/main/packages/llm/llm_tag_parser_dart

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
# path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
